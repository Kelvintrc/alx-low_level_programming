#include <stdio.h>
/**
 *main - Contains the code for program execution
 *
 *Return: returns 0(Success), otherwise non-zero
 *
 */

int main(void)
{
	int i = 0;

	while (i < 10)
	{
		printf("%d", i);
		i++;
	}
	printf("\n");
	return (0);
}
